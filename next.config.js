/**
 * @type {import('next').NextConfig}
 */
const config = {
  reactStrictMode: true,
  images: {
    domains: ['tortugablava.es', 'www.projectpieta.com', 'modastentacion.com', 's.cdnmpro.com'],
  },
};

module.exports = config;
